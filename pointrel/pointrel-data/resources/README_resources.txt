Pointrel resources go here.

Resources are immutable files which have been added to the archive. They are never deleted in normal operations.
They are given a name that reflects a hash of the contents.